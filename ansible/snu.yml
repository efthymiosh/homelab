- hosts: snus
  become: true
  vars:
    nomad_version: "1.2.5"
    consul_version: "1.11.2"
  vars_files:
  - "vars/consul.yml"
  - "vars/consul_services.yml"
  - "vars/nomad.yml"
  - "vars/minio.yml"
  - "vars/moosefs.yml"
  roles:
  - role: docker
    tags: docker
  - role: consul
    tags: consul
  - role: minio
    tags: minio
  - role: nomad
    tags: nomad
  - role: tailscale
    tags: tailscale
