- hosts: snus
  become: true
  vars:
    nomad_version: "1.7.2"
    consul_version: "1.17.1"
  vars_files:
  - "vars/consul_aio.yml"
  - "vars/consul_services.yml"
  - "vars/nomad_aio.yml"
  - "vars/minio.yml"
  - "vars/vault.yml"
  - "vars/vault_agent.yml"
  roles:
  - role: docker
    tags: docker
  - role: consul
    tags: consul
  - role: minio
    tags: minio
  - role: nomad
    tags: nomad
  - role: tailscale
    tags: tailscale
  - role: vault/server
    tags: vault
  - role: vault/agent
    tags: vault-agent
